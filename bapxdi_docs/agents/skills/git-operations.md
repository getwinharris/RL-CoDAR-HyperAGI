# Agent Skill: GIT-OPERATIONS

SKILL: git-operations
KEYWORDS: git commit push pull branch merge clone repository version control
TYPE: capability

DESCRIPTION:
This skill enables agents to perform git operations operations.
When a query requires git operations the agent activates this skill.
Document bytes encode the git operations knowledge pattern.

QUERY_TRIGGERS: git commit push pull branch merge clone repository version control

USAGE:
Agent reads this skill doc via RLM when git operations is needed.
Diffusion over skill bytes produces git operations outputs.
No external API required — document IS the skill implementation knowledge.
